# Script Entrypoints

Top-level files in this directory are public command entrypoints. The main
training command is:

- `lerobot_train.py`

Additional utilities:

- `lerobot_info.py`: dataset/model metadata inspection.
- `fastwam/` and `fastwam_*.py`: FastWAM comparison-method utilities.
